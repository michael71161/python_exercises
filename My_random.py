import random

random_side = random.randint(0, 1)
if random_side == 1:
  print("Heads")
else:
  print("Tails")

#we just need to rum the program and to see at the output tails or heads in a random order 
